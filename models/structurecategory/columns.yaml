# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    name:
        label: Nom

    description:
        label: Description

    structure:
        label: Structure
        relation: structure
        select: name
